package com.c2v4.colonize.domain

val testState = State(
        wallets = mapOf(Player("Asd") to mapOf(Resource.HEAT to 3, Resource.ENERGY to 4, Resource.PLANT to 2),
                Player("Bsd") to mapOf(Resource.ENERGY to 1, Resource.HEAT to 3)))